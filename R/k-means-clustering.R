#' Example on how to use k-means clustering
#' @importFrom assertthat assert_that
#' @param arg ; A numeric vector to calculate the arithmetic mean over.
#' @return numeric ; The Arithmetic mean of 'src.vector'.
#' @export
k.means.clustering <- function(arg) {
  # assert_that(is.numeric(src.vector))
  return(arg)
}
